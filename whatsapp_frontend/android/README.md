This app uses Expo managed workflow. There is intentionally no native Android project here.
If you need to generate it locally, run:
  npm run prebuild:android

CI SHOULD NOT attempt to run any Gradle tasks in this directory.
