buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.4")
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

// This is a stub build.gradle to prevent CI failures for an Expo-managed app.
// Real Gradle files are generated by `expo prebuild`.
task ciNoop {
    doLast {
        println "Expo-managed app: skipping native build tasks in CI (stub build.gradle)"
    }
}
