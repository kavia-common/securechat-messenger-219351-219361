#!/usr/bin/env bash
# Root-level Gradle wrapper stub for CI environments that invoke ./gradlew from the repository root.
# This is an Expo-managed project without native Android build in CI.
echo "Root Gradle wrapper stub: No native Android build for this Expo-managed project."
echo "Skipping Gradle task: $@"
exit 0
